[project]
name = "multiply-local-strands"
description = "Self-contained local Strands multiply rollout"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "osmosis-ai[server]>=0.2.20",
    "strands-agents",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
