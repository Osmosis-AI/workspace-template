# Multiply training - local OpenAI Agents backend.
# Command: osmosis train submit configs/training/multiply-local-openai.toml

[experiment]
rollout = "multiply-local-openai"
entrypoint = "main.py"
model_path = "Qwen/Qwen3.6-35B-A3B"
# Training datasets are platform dataset names, not local data/*.jsonl paths.
dataset = "multiply_1k-2026-04-13-66b645-2026-05-06-dffd06"
# commit_sha =

[training]
# lr = 1e-6
# total_epochs = 1
n_samples_per_prompt = 8
rollout_batch_size = 16
# max_prompt_length = 8192
# max_response_length = 8192

[sampling]
# rollout_temperature = 1.0
# rollout_top_p = 1.0

[checkpoints]
# eval_interval =
# checkpoint_save_freq = 20
