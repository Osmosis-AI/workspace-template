[project]
name = "workspace-template"
description = "Osmosis workspace repository with training rollouts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "osmosis-ai[eval]>=0.3.2,<0.4",
]
